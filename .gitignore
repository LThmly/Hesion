# Dependencies & build
node_modules/
dist/
dist-electron/
release/
*.tsbuildinfo

# Logs & OS
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Env / secrets (API keys live in ~/.config/hesion/ — never commit them)
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets/

# Editors / local tooling
.vscode/
.idea/
.cursor/
*.swp
*~

# Electron / Chromium leftovers if ever written into the tree
DevToolsActivePort
blob_storage/
Cache/
Code Cache/
GPUCache/
Local Storage/
Session Storage/
